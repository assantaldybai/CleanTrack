# Test Credentials
# Agent writes here when creating/modifying auth credentials (admin accounts, test users).
# Testing agent reads this before auth tests. Fork/continuation agents read on startup.

## SKYX Beta Demo Accounts
- Super Admin: username `superadmin`, password `Super2025!`
- Organization Admin: username `org_gazprom`, password `Org2025!`
- Cleaning Company Admin: username `cleaning_admin`, password `Clean2025!`
- Cleaner: username `cleaner_maria`, password `Cleaner2025!`
